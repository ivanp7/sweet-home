. "$HOME/.local/xdg_paths.shell"

if [ -z "${USERPATH:-}" ]
then
    export USERPATH="$USER_BINARIES_PATH:$USER_BINARIES_PATH/local"
    export PATH="$PATH:$USERPATH"
    [ "$(id -u)" -ne 0 ] || export ROOTPATH="$ROOTPATH:$USERPATH"
fi

